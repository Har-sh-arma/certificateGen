Make sure the dependencies are installed

use the following Command
pip install ironpdf PIL pandas

Save The Certificate Design as "certificate.pdf" in the template directory.

Put the EXcel Sheet Containing the Names under the 
"Names" Column in the root directory(named as cert.xlsx).

Run the Batch Script in the root folder
